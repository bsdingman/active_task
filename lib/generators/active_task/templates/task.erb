class <%= @time %>_<%= name.camelize %> < ActiveTask::Task
  # TODO: RAKE DOCS
  # execute(:rake, "task", "namespace:task")

  # TODO: COMMAND DOCS
  # execute(:command, "yarn install", "bundle install")

  # TODO: CODE DOCS
  # execute(:code, :my_method)
end